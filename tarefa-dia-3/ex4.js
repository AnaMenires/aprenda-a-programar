let multipicador = 9
let n = 10
let i = 1

while (i <= n ) {
   let resultado = multipicador * i 
   let textoDeExibicao = `${multipicador} x ${i} = ${resultado}`
   console.log (textoDeExibicao)
   i++
}